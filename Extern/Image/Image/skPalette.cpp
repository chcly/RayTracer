/*
-------------------------------------------------------------------------------
    Copyright (c) Charles Carley.

  This software is provided 'as-is', without any express or implied
  warranty. In no event will the authors be held liable for any damages
  arising from the use of this software.

  Permission is granted to anyone to use this software for any purpose,
  including commercial applications, and to alter it and redistribute it
  freely, subject to the following restrictions:

  1. The origin of this software must not be misrepresented; you must not
     claim that you wrote the original software. If you use this software
     in a product, an acknowledgment in the product documentation would be
     appreciated but is not required.
  2. Altered source versions must be plainly marked as such, and must not be
     misrepresented as being the original software.
  3. This notice may not be removed or altered from any source distribution.
-------------------------------------------------------------------------------
*/
#include "Image/skPalette.h"

const skPixel skPalette::Red     = skPixel(0xFF0000FF);
const skPixel skPalette::Orange  = skPixel(0xFF8000FF);
const skPixel skPalette::Yellow  = skPixel(0xFFFF00FF);
const skPixel skPalette::Green   = skPixel(0x00FF00FF);
const skPixel skPalette::Cyan    = skPixel(0x00FFFFFF);
const skPixel skPalette::Blue    = skPixel(0x0000FFFF);
const skPixel skPalette::Magenta = skPixel(0xFF00FFFF);
const skPixel skPalette::Black   = skPixel(0x000000FF);
const skPixel skPalette::Grey01  = skPixel(0x101010FF);
const skPixel skPalette::Grey02  = skPixel(0x303030FF);
const skPixel skPalette::Grey03  = skPixel(0x505050FF);
const skPixel skPalette::Grey04  = skPixel(0x707070FF);
const skPixel skPalette::Grey05  = skPixel(0x909090FF);
const skPixel skPalette::Grey06  = skPixel(0xB0B0B0FF);
const skPixel skPalette::Grey07  = skPixel(0xD0D0D0FF);
const skPixel skPalette::Grey08  = skPixel(0xE0E0E0FF);
const skPixel skPalette::Grey09  = skPixel(0xF0F0F0FF);
const skPixel skPalette::White   = skPixel(0xFFFFFFFF);
